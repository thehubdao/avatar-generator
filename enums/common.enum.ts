export enum GlobalValues {
  BaseCampaign = 'base',
  Acc = 'Accessories',
  Config = 'Config',
}

export enum AttributeValues {
  Campaign = 'campaign',
}

export enum ViewModuleState {
  SwitchingModule,
  OnModule
}