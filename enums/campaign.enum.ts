export enum ClientQuestion {
    Name = 1,
    BGColor = 2,
    Gender = 3,
}

export enum CampaignConfigOption {
    Base = 'base',
    Color = 'color',
    Camera = 'camera',
    Light = 'light',
}

export enum CameraConfigOption {
    DefCam = 'defCam',
}