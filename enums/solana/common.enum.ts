export enum SolanaSections {
  View,
  Collection,
  LeaderBoard,
  Play,
  Mint
}

export enum SolanaCollectionSections {
  CITIZENS,
  WEARABLES
}

export enum PaymentType {
  SOL = "SOL",
  TOKEN = "TOKEN"
} 