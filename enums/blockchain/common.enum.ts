export enum Blockchain {
    Lukso = "lukso",
    Solana = "solana",
    Ethereum = "ethereum",
    Root = "root"
}

export enum LoginLibrary{ //TODO: Add here each new library used to login
    Privy = "privy",
    Pass = "pass"
}